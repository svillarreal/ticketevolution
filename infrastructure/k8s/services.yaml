apiVersion: v1
kind: Service
metadata:
  name: ticketevol-be-svc
  namespace: ticketevol-be
spec:
  selector:
    app: ticketevolapp-be
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
