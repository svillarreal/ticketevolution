apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticketevolapp-be-deployment
  namespace: ticketevol-be
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ticketevolapp-be
  template:
    metadata:
      labels:
        app: ticketevolapp-be
    spec:
      # volumes:
      #   - name: ticketevol-pg-postgresql-secret-volume
      #     secret:
      #       secretName: ticketevol-pg-postgresql
      containers:
        - name: ticketevolapp-be
          image: shiryuknight/ticketevolution-be:v1.0.2
          imagePullPolicy: Always
          # volumeMounts:
          #   - mountPath: "/etc/ticketevol-pg-postgresql-secret-volume"
          #     readOnly: true
          #     name: ticketevol-pg-postgresql-secret-volume
          ports:
            - containerPort: 3000
          env:
            - name: CONTAINER
              value: k8s
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  key: password
                  name: ticketevol-pg-postgresql
            - name: PGHOST
              value: ticketevol-pg-postgresql-hl.ticketevol-be.svc.cluster.local
            - name: PGPORT
              valueFrom:
                secretKeyRef:
                  key: port
                  name: ticketevol-pg-postgresql
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  key: database
                  name: ticketevol-pg-postgresql
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  key: username
                  name: ticketevol-pg-postgresql
          resources:
            requests:
              cpu: "250m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
